version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: sweetshop-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sweetshop
      MYSQL_USER: sweetuser
      MYSQL_PASSWORD: sweetpass
    ports:
      - "3307:3306"
